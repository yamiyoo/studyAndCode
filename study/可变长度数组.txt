数组名称不可代表存储区
数组名称代表数组存储区第一个存储区地址
printf（"%p %p",arr,&arr[]）
显示出来结果一样
对数组名称sizeof计算：整个数组包含数组个数
printf("%d",sizeof(arr))
   计算数组位数*类型所占字节
C99规范里允许申明数组时候使用变量表示数组里的存储区个数
这种数组叫变长数组
 scanf("%d",&a);
 int arr[a];
使用变量数组：gcc -std=c99 程序.c
如果程序多次运行则每次运行时数组里的存储区个数可以不同
每次运行过程中存储区个数不同


