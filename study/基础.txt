day01.5

电信:电磁系统通信
BSS:业务支撑系统,面向运营商和服务(面向用户):CRM\Biling\BI
OSS:运营支撑系统,提供技术支撑管理:资源管理\服务保障\服务开通

BSS决定OSS OSS影响BSS速度

CRM系统功能架构(八大功能领域)
HR交换机,归属集系统
全国系统交互:dsmp;MIS:HR:OA
业务支撑CRM BOSS
运营分析BASS

三户模型
个人\家庭\政企模型--客户 账户 用户

设计>>架构

三层: 前端 中台(逻辑层) 数据层
接入层 访问层 核心应用 核心数据
负载均衡(根据算法请求应用,应用根据情况关掉功能):软硬代理
会话(全局同意管理)
集成架构:网络主机负载均衡安全
x86(接入口\应用层)

缓存:高频,数据低速变化,
	降低数据库读写,提高利用率
	本地缓存 主机缓存 自研数据库

网元 _verisBilling_ CRM 

高性能多级缓存:进程内存,XC主机,MDB内存数据库

CRM 数据拆分(中心库\地州库\日志库)___生命周期管理
数据存储原则:模块换,碎片化
规划设计:分表分区分库
管理规划:搬迁
离线规划



day01.2

CRM系统功能框架
渠道管理:管理营业厅

三户模型:
客户
账户
用户


号码和sim卡归属一个交换机

day02.1
维护:
 个人业务:针对客户产生服务 集团业务 宽带业务 新型业务
 维护:平台,主机,脚本语言
 计费
ngboss :开发个人集团宽带
		测试研发

CRM 客户关系管理 + Boss 计费 = ngboss

三成架构 数据 访问 控制
网络(外网) 
		异构防火墙 
硬件级负载均衡器(类似阿帕奇:代理)
		(接入层)
异地载入防止意外
web服务器--会话服务器(存储信息)
分布搜索缓存会话请求(核心应用)
读数据库

app集合功能但web没有集合:保持事务是一个,模块整合
事务:做事所经过有不同的人的节点

用户交互层
	服务编排层
能力中心
数据访问层
数据分布式存储

根据web服务由欠费和缴费创建不同服务





shell脚本
h(格式)
-n检测运行
-r 递归
nohub ./start.h挂在服务器端
/查找内容(向下找)
?查找内容(向上找)
rpm -qa查找已安装软件包
alias 新操作 = '操作'//简化操作
su-切黄金变量

中级

find 路径 -name "*.txt"
find 路径 type d -name "abc" #路径
			-user
find -type f -name cat{} \;
grep查找报错
	-i -A(after) -B(befer)
du 磁盘空间查看
swap被占用说明内存不够用
awk文本解析(分割文本)
	awk '{$1$2}'
	awk -F "分割条件"
strftime(时间戳)

pwd 存储用户信息
wc -l 统计行数
sed 在线编辑
管道  前面输出作为后面输入 |grep
sed 's/^.addr://g'
-u uniq 去重
sort 排序 -nrk 倒序
top free 查看主机性能
#netstat

shell编程
./ 执行后需要授权
sh 执行语法无错的文本,默认用户执行

dev/null清除进linux空洞内

结构体写法
将pwd操作赋给var
var = 'pwd'
var = $(pwd)
取值给var
var = ${待取得值}










