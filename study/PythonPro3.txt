抽奖界面
GUI( 图形界面化)
	python语言》制作软件
	控制电脑
理想效果
原理分析
  用Python开发一个图形处理化界面程序
  12个随机结果一集两个功能选项
  界面布置
  功能描述
需求开发（新程序员） 项目优化（老程序员）
import tkinter#导入pythonGUI库
import threading
import time

#生成主窗口
root = tkinter.Tk()
#图形界面名字
root.title('题目')
root.minsize(300,300)#图形大小

btn1 = thinner.Button(root,test='name1',bg='red')
btn1.place(x=20,y=20,width=50,height=50)
but2...butn
root.mainloop()
butnLists = [btn1,btn2...]
isloop = False#是否开启循环
stopsign = False#是否停止
stopid = None
def round():
	global isloop
	global stopid

	if isloop==True
	   return
	i= 1
	if isinstance(stopid,int)
#isinstance判断类型，考虑继承关系（推荐）
#isinstance（变量名，int）返回true或false
#type()不考虑集成
	   i= stopid
#旋转
while True
   time.sleep(0.03)
#所以组件变色
	for x in btnLists
	    x['bg']='withe'
    btnLists[i]['bg']='red'
	i+=1
	print('当前i'，i)
	if stopsign==True；
	isloop = False
	stopid = i
	break
#开始按钮

