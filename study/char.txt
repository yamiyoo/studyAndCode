c 语言所有文字信息必须记录在一组连续的
	字符类型存储区
所有文字信息必须以'\0'字符作为结尾,这个
	字符ASCII 码就是0
符合以上两个特征的内容是字符串,它可以用
	来表示文字信息
字符串'\0'前有效文字信息
所有字符串都可使用一个字符类型指针表示

字符串字面值是一种字符串,用两个双引号
	中间一组字符("abc","^&&"等)
编译器变异的时候会在字符串字面值的末尾
	自动添加'\0'字符

